import { describe, test } from "bun:test";
import { Uint } from "../src/uint/uint.js";
/*
describe("Bitwise operators", () => {
    test("Bitwise AND", () => {
        console.log(Uint.from("ffffffffffffff") == 0xffffffffffffffn);
        const buf3 = Buffer.alloc(1);
        buf3[0].valueOf()
    });
});
*/